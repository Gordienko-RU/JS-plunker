const GenericTreeNode = require('./generic-tree-node');

// TODO: implement recursive Depth First Search to perform object transformation to Tree 
// const buildTreeFromObject = (obj) => {
//   const rootKey = Object.keys(obj)[0];
//   const rootNode = GenericTreeNode(rootKey);
//   const bodyTreeKeys = Object.keys(obj[rootKey]);

//   bodyTreeKeys.forEach((key) => );
// }
